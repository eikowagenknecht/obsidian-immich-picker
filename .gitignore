# vscode
.vscode

# Intellij
*.iml
.idea

# npm
node_modules

# Don't include the compiled main.js file in the repo.
# They should be uploaded to GitHub releases instead.
main.js

# Exclude sourcemaps
*.map

# obsidian
data.json

# Local test vault config. The notes are tracked; the vault's own settings and
# the plugin build copied into it are not — that copy of styles.css only drifts
# out of sync with the real one at the repo root.
test-vault/.obsidian/

# Exclude macOS Finder (System Explorer) View States
.DS_Store

# Windows
Thumbs.db

# Claude Code
.claude
